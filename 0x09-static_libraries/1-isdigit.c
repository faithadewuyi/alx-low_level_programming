#include "main.h"

/**
 * _isdigit - function that checks digits (0 to 9)
 * @c: interger to check
 * Return: 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
